# P0 改进说明 · 认知障碍早期筛查原型

> 配套代码：`ad-ouc-prototype/`（scales.js / app.js / styles.css / index.html）
> 日期：2026-09-08

---

## 一、P0 四项总览

| #   | 原问题                                               | 本次改进                                                                                       | 涉及文件                        |
| --- | ---------------------------------------------------- | ---------------------------------------------------------------------------------------------- | ------------------------------- |
| 1   | AVLT 把 15 个词全屏显示 = 开卷考试，且无限时延迟回忆 | 主试专用词表面板（受试者不可见）+ 3 次学习 + 干扰 + 延迟倒计时节点 + 再认九宫格                | scales.js / app.js / styles.css |
| 2   | HAMD/HAMA 曾用「表情尺映射」方法学错误               | 改为 0~4 / 0~2 主试数字评分，并为每题加标准评分锚点（SIGH 结构化追问依据）                     | scales.js                       |
| 3   | MMSE 仅 9 题、HAMD 7 题、HAMA 5 题，非完整量表       | 扩为 MMSE 标准 30 分版、HAM-D17（17 项）、HAMA-14（14 项）                                     | scales.js                       |
| 4   | CDR 只按 6 域求和（SB）分级，未实现 Global 规则      | 引擎内实现华盛顿大学 CDR Global 判定（0/0.5/1/2/3），结果同时给 SB 与 Global，导出加 Global 列 | scales.js / app.js              |

---

## 二、逐项说明

### ① AVLT 防泄题 + 延迟回忆调度

**改进了什么**

- 新增 `reveal` 字段：每个学习/干扰试次配置一个「主试专用词表」面板，仅当 `state.role==="rater"` 时渲染，受试者（自评/家属通道）完全看不到词。面板默认折叠，主试点「显示词表」才展开。
- 新增 `delay` 节点类型：延迟回忆前插入倒计时环节，临床设定 20 分钟（`seconds:1200`），演示用 15 秒（`demoSeconds:15`）代替；计时结束自动进入延迟回忆，并提供「跳过等待（演示用）」。
- 新增 `recog` 节点类型：再认环节把 12 个目标词 + 12 个干扰词洗牌后排成九宫格，受试者/主试点选，引擎统计正确数与假阳性数。
- 学流程变为：t1/t2/t3 三次学习回忆 → t4 干扰列表立即回忆 → delay 等待 → n4 延迟回忆（主指标）→ n5 再认。
- 计分除延迟回忆灯色外，结果里额外给出「学习 3 次累计词数」与「再认正确数/12」。

**还需要什么**

- 真实临床应固定词表版本与标准化朗读节奏（目前词表为华山版常用 12 词，可在 `window.AVLT_TARGET` 一处集中替换）。
- 干扰列表目前仅做「立即回忆词数」，未做干扰列表本身的延迟再认；如需更严谨可再补一项。
- 倒计时目前为前端演示计时，未做「到点自动弹窗锁屏」的强约束。

**为什么这样做**

- 原版把词一次性全屏列出，受试者等于开着书考试，回忆分数无临床意义——这是评审指出的硬伤。
- 延迟回忆是区分正常老化与早期 AD 的关键指标（HV 华山版核心），原版缺失。
- 防泄题的核心不是「不显示词」，而是「词只进主试耳朵、不进受试者眼睛」，所以采用主试专属折叠面板 + 回忆计数/再认点选，受试端零暴露。

**有什么好处**

- 数据可信度从「开卷」升级为「闭卷 + 延迟 + 再认」三段式，演示时经得起老师追问。
- 机制可演示、可跳过，不拖慢答辩节奏。
- 词表面板复用 `reveal` 字段，未来其它需主试口述的量表（如 RAVLT）可直接套用。

---

### ② HAMD / HAMA 主试模式锚定

**改进了什么**

- 保留并明确「主试模式」定位：intro 直接写明「由受过训练的主试根据结构化提问与临床观察逐题评分，不依赖受试者自评」。
- 每题新增 `anchors` 评分锚点（如 HAMD 抑郁情绪：0 无 / 1 只在问答中流露 / 2 自发诉说 / 3 与环境不协调 / 4 极重无法交谈；HAMA 14 项各 0~4 锚点）。
- 渲染时，数字录入框上方展示「评分锚点（主试参考）」面板，把 0/1/2/3/4 各自含义列清楚。

**还需要什么**

- 锚点文字为通用版本，若对接具体研究方案，应替换为该方案的标准提问脚本。
- 未做「主试/受试者双端界面」的物理隔离（目前靠角色切换 + 面板可见性约束），如需更严谨可加主试密码态。

**为什么这样做**

- 早期评审指出「用表情尺（emoji 0~4）映射 HAMD/HAMA 是方法学错误」：这两份量表是**他评量表**，必须由主试按观察与结构化追问打分，不能用受试者自评的表情尺。
- 纯数字 0~4 仍可能被误读为「凭感觉打分」，所以补上锚点，把评分依据显式化，体现 SIGH 结构化风格。

**有什么好处**

- 方法学正确：从「自评表情尺」纠正为「主试结构化评分 + 锚点」，答辩时能明确说清已解决该评审意见。
- 实操友好：主试边看锚点边打分，减少评分漂移，提升演示专业度。

---

### ③ 关键量表题量补齐（MMSE / HAMD / HAMA）

**改进了什么**

- **MMSE**：由 9 道精简题扩为**标准 30 分版**，覆盖时间定向(5) + 地点定向(5) + 登记(3) + 注意计算(5) + 延迟回忆(3) + 命名(2) + 复述(1) + 三步指令(3) + 阅读(1) + 书写(1) + 临摹(1)。原型以 **24 个对话步**承载这 30 分（多分点任务拆成多个 1 分小步，总分仍为 30），比「硬凑 30 个独立小题」更贴近真实 MMSE 结构。
- **HAMD**：由 7 题扩为 **HAM-D17 标准 17 项**，含 0~4 与 0~2 两种权重，满分 50，阈值按 17 项标准分段（<7 / 7~17 / ≥18）。
- **HAMA**：由 5 题扩为 **HAMA-14 标准 14 项**，每项 0~4，满分 56，阈值按 14 项分段（<7 / 7~13 / ≥14）。

**还需要什么**

- MMSE 单项仍按固定分段给灯，**缺年龄×教育年限常模**（如低教育者 cutoff 应下调），需在报告/导出处标注「阈值未常模校正」。
- 部分操作类题目（画钟、临摹、三步指令）在纯聊天原型里只能「主试判断后录 0/1」，无法自动判分——这是原型形态固有限制。
- 完整施测指导语（如「100 减 7」连续 5 次）建议补进各题 hint 或单独的施测说明。

**为什么这样做**

- 课程作业要求「演示可信度」，题量贴近真实量表是性价比最高的加分项：不用改引擎，只改 `scales.js` 配置即可。
- 标准版题量让老师在演示时看到的是「真量表」而非「玩具题」，质疑成本大幅降低。

**有什么好处**

- 三个核心量表从「示意」升级为「可对照标准计分」，专业度肉眼可见提升。
- 仍保持零引擎改动：新增/扩题只在配置层，符合原架构「决策树与引擎解耦」的设计。

---

### ④ CDR Global 规则

**改进了什么**

- 引擎 `compute()` 内新增 `cdrGlobal(boxes)` 函数，实现华盛顿大学 CDR Global 判定逻辑：
  - 六域全 0 → Global 0
  - 仅记忆 0.5、余皆 0 → Global 0.5
  - 记忆 ≥1 且至少 1 个其它域 ≥0.5 → Global 1
  - 记忆 ≥2 且至少 1 个其它域 ≥1、且 ≥2 个域 ≥0.5 → Global 2
  - 记忆 =3 且 ≥3 个其它域 =3 → Global 3
  - 其余兜底按记忆域近似
- CDR 结果灯色与标签改为以 **Global** 为准（0/0.5 绿、1 黄、2/3 红），对话结果同时给出「CDR-SB X · Global Y（名称）」。
- 导出 Excel 增加 **CDR-Global** 独立列。

**还需要什么**

- 当前为「演示实现」的判定近似；精确 Global 应严格按官方查表（含更细的域组合约束），建议在文档/代码注释中标注。
- 未做「CDR-SB 与 Global 不一致」的冲突提示（如 SB 高但 Global 低），可作为后续校验项。

**为什么这样做**

- 原版只按六域求和（CDR-SB）分级，而临床与研究中 **Global CDR** 才是分期（正常/可疑/轻/中/重）的金标准，SB 只是辅助。
- 华盛顿大学规则以**记忆域为主导**（记忆不行则整体不行），直接求和会掩盖这一逻辑。

**有什么好处**

- 分期结果更符合临床惯例，报告/导出里一眼能看到 Global 分级。
- 实现在引擎层、对配置零侵入，其它量表不受影响。

---

## 三、已做的验证

- `node --check` 通过 `scales.js` 与 `app.js` 语法校验。
- 数据层逻辑测试（临时脚本，已清理）确认：
  - MMSE 24 对话步 / HAMD 17 / HAMA 14 / AVLT 7 项 / CDR 6 域，结构正确；
  - AVLT 目标词 12、干扰词 12、零重合；reveal 词表面板真实取到 12 词；
  - `cdrGlobal` 六组样例（0 / 0.5 / 1 / 2 / 3 / 兜底）全部符合预期。
- 修复一处会**同时影响浏览器运行**的真实缺陷：原 `scales.js` 在 `window.SCALES` 内引用 `AVLT_TARGET`，但该常量定义在文件末尾、且非全局变量，浏览器中同样会抛 `ReferenceError`。已把词表定义上移并改用 `window.AVLT_TARGET` 引用。

## 四、文件改动清单

- `scales.js`：重写 AVLT / HAMD / HAMA / MMSE / CDR 五个量表配置；词表常量上移；新增 `kind: recog / delay`、`reveal`、`anchors` 字段说明。
- `app.js`：`compute()` 支持 recog 计分与 CDR Global、附带 details；`renderComposer()` 新增 reveal 面板、anchors 面板、`delay`、`recog` 四个分支；新增 `cdrGlobal / revealPanel / anchorPanel / delayBlock / recogBlock`；结果与导出补充 details / Global。
- `styles.css`：新增 `.reveal-panel`、`.anchor-panel`、`.delay-wrap`、`.recog-grid/.recog-word` 样式（含移动端 3 列适配）。
- `index.html`：无改动（结构已支持）。
