# 认知障碍早期诊断临床数据采集系统 · 工程日志

## 日志维护约定

- 每次代码、样式、配置或文档更新都追加记录，不删除真实历史。
- 每个错误记录“现象 → 原因 → 修复 → 验证”；未解决问题标记为“待处理”。
- 页面行为改动同时记录验收路径和已知限制。
- 日期使用 `YYYY-MM-DD`，同一天按处理顺序编号。

## 2026-09-04

### 1. 需求范围收敛

- 前端负责患者基础信息填写、患者自评和基础信息咨询。
- 因为是短期小组作业，将患者和自测者合并为“受试者 / 自评通道”。
- 保留家属 / 知情者通道和医生 / 科研工作站；自评支持模块级跳过，跳过不计为 0 分。

### 2. 参考站点分析

- 参考：[https://ad-scd.ai.studio/](https://ad-scd.ai.studio/)。
- 借鉴三通道门户、阶段导航、语音朗读、自动缓存、大字模式和任务入口卡片。
- 未复制医院品牌、页面内容或第三方素材。

### 3. UI 与交互打磨

- 修改 `index.html`、`app.js`、`styles.css`。
- 顶栏增加品牌、身份、保存状态和在线 / 离线状态。
- 增加 `.bigbtn` 的 `primary`、`secondary`、`guide` 变体；主操作支持低频呼吸提示。
- 优化通道卡片、聊天气泡、输入框、量表卡片、报告浮层、键盘焦点态和窄屏适配。
- 使用 `prefers-reduced-motion` 降级动画；未引入 CDN 或重量级框架。

### 4. 角色状态问题与修复

- 问题：旧的 `localStorage` 角色会污染新页面，医生建档可能沿用患者身份。
- 修复：建档明确设置医生上下文；初始化校正 `role/channel`；受试者增加基础信息入口。
- 验证：通过 `node --check`；待手工验收缓存恢复路径。

### 5. 技术文档补充

- README 增加技术框架、UI 复用约定、Mermaid 流程图、浏览器 API 和外部 API 预留说明。
- 明确当前版本只使用本地存储，不上传患者数据。

### 6. 错误：角色切换后页面未重置

- 现象：切换角色后聊天内容未变，但字体和输入框大小变化。
- 原因：只更新 `role/channel`，未清理当前 `view/flow/messages/answers`。
- 处理：角色切换应保存草稿、停止计时器、回到新角色首页，不能让不同角色共用当前聊天流程。

### 7. 修复：角色切换增加离开确认与按角色草稿

- 未完成建档或量表时弹出“当前流程已保存，是否离开”。
- 取消：恢复原角色并继续；确认：按角色保存草稿、停止计时器、进入新角色首页。
- 切回原角色时恢复对应草稿；患者资料和已完成结果保留。

### 8. 更新：问答互动、重点提示与基础信息入口

- 消息增加轻量进场动画、重点气泡高亮和题目进度条。
- 姓名、出生年份、教育年限、身高、体重和手机号增加合理性校验。
- 医生和家属通道增加“录入患者基础信息”；错误提示显示在输入区。

### 9. 错误：数字输入校验正则转义层级

- 静态复核发现数字正则的编辑转义可能误判合法数字。
- 修正正则字面量，并给数字输入补充 `min` 属性。
- 验证：`node --check ad-ouc-prototype/app.js`、`git diff --check` 通过。

### 10. 更新：当前问题与历史对话视觉分层

- 最后一条系统问题标记为 `current`，使用大字号、浅黄色、高对比边框和“正在回答”标签。
- 历史消息降低透明度和阴影；底部提示“请回答上方高亮问题”。

### 11. 更新：医生 / 家属表格导入与缺失信息补填

- 支持本地读取 CSV、TSV 和本系统导出的 HTML 型 `.xls`。
- 自动识别常见中文 / 英文表头，优先按研究编号或姓名匹配；缺失或不合理字段进入补填对话。
- 不解析二进制 `.xlsx`，不上传患者文件。
- 修复：表格未提供性别时不再默认填为男性。

### 12. 修复：可选字段无法跳过

- 手机号为可选字段时增加“暂不填写”按钮；必填字段仍需通过校验。

### 13. 更新：对话重点字段高亮

- 姓名、性别、出生年份、年龄、教育年限、身高、体重、电话、研究编号、总分等字段在气泡内部单独高亮。
- 异常 / 未完成为红色，临界 / 已跳过为橙色，正常范围为绿色。
- 使用 DOM 文本节点拼接，避免把用户输入直接写入 `innerHTML`。

### 14. 更新：前后端接口契约

- 新增 `API接口设计.md`，定义 `/api/v1` REST + JSON 契约。
- 覆盖患者档案、表格导入预览 / 确认、缺失字段、草稿、量表答案、完成 / 跳过、报告、导出和 FAQ。
- 文档包含字段枚举、错误码、幂等、版本冲突和安全要求。

### 15. 更新：表格缺失字段交由患者补填

- 表格导入后，医生 / 家属可点击“由患者本人补填”。
- 保留已识别信息和患者匹配关系，切换受试者大字模式，只展示缺失 / 不合理字段。
- 补填完成后更新同一档案，不新增重复患者，不覆盖已完成量表结果。
- 可选字段没有值时允许“暂不填写”。

### 16. 更新：前后端交付前整体检查

- 修正根 README 的错误端口：`localhost:8124` 改为 `127.0.0.1:8123`。
- 修正原型 README 的访问地址，和当前静态服务保持一致。
- 清理工程日志重复条目和编号错位，保留连续历史记录。
- 复核待处理事项：本机浏览器截图回归、二进制 `.xlsx`、完整 FAQ 仍需后续实现或手工验收；模块级自评跳过已在第 17 条补齐。

### 17. 修复：整体检查发现的流程与安全问题

- 现象：文档声明自评可跳过，但页面没有跳过入口；受试者未建档时可直接开始量表，结果无法关联患者；切换角色后草稿可能找不到原患者；报告 / 导出拼接用户输入存在 HTML 注入风险。
- 原因：跳过状态和返回操作未实现；量表入口缺少基础信息前置判断；草稿恢复只比较当前 `state.patient`；部分展示模板直接拼接动态文本。
- 修复：新增“跳过本人自评”入口和返回首页按钮，记录 `skippedScales` 且不生成分数；受试者无档案时先进入基础信息；按 `patientCode` 从患者列表恢复草稿关联；报告与 `.xls` 导出统一使用 HTML 转义。
- 验证：通过 `node --check ad-ouc-prototype/app.js`、`git diff --check`；浏览器手工验收路径已补充到 README。

### 18. 错误：推送远程 v1 分支权限不足

- 现象：本地提交 `498c0e3` 已创建，但 `git push origin HEAD:v1` 未创建远程分支。
- 原因：GitHub 返回 `403 Permission to MH-138/ad-ouc.git denied to Cwjlearner`，当前凭据对目标仓库没有写权限。
- 处理：保留本地 `v1` 分支和提交，未修改或删除任何远程内容；获得仓库写权限后执行 `git push origin v1`。
- 验证：`git ls-remote --heads origin` 当前仅返回 `main`；本地工作区干净，提交哈希为 `498c0e3b6ac27058645fe54b9562f6a0e7ce767c`。

### 19. 修复：医生建档按钮空白及角色切换残留页面

- 现象：医生点击“录入患者基础信息”后页面一片空白；在已完成量表、量表菜单或导出页切换角色，可能仍停留在旧聊天页面。
- 原因：医生入口直接绑定 `startIntake`，浏览器传入 `MouseEvent`，导致角色状态被写成事件对象并触发渲染异常；角色切换只在“进行中流程”时重置视图。
- 修复：医生入口改为显式调用 `startIntake("rater")`；`startIntake` 对非字符串角色统一降级；角色切换始终清理旧视图并进入新角色首页；顶部返回首页时先保存进行中的流程草稿。
- 验证：通过 `node --check ad-ouc-prototype/app.js`、`git diff --check`。

### 20. 更新：全站按钮与状态转换审计

- 检查范围：顶部返回首页、角色切换、三通道卡片、基础信息、自评跳过、量表卡片、继续上次、表格导入、患者接力、报告、导出、错误重试和完成页按钮。
- 发现并修复：量表菜单、导出菜单和首页量表卡片缺少视图切换；未建档家属提前看到量表卡片；返回通道选择未持久化；从通道选择页进入新通道时可能残留上一通道流程；未完成建档草稿因没有患者对象而不显示“继续上次”；量表完成提示在刷新时可能重复写入。
- 入口收敛：删除只有一个自评量表时重复的“查看可用自评量表”；家属量表在建档前隐藏；医生导出在没有患者数据时隐藏；导出页仅在存在当前患者时显示“导出当前患者”。
- 状态提示修复：只有存在未回答题目时才显示“正在回答”和黄色当前题样式；导出、跳过、错误、菜单及完成页不再显示“请回答上方高亮问题”。
- 结论：其余入口均有明确的成功、空数据或错误返回状态；无患者档案时不会暴露无法正常归档的量表入口。
- 重点约束：医生 / 家属建档入口必须显式传入角色字符串；所有流程跳转必须同步设置 `view`、`flowType`、`flow` 和草稿状态。
- 文档：在 `ad-ouc-prototype/README.md` 新增“按钮与状态审计”表，记录保留理由和异常处理方式。
- 验证：通过 `node --check ad-ouc-prototype/app.js`、`git diff --check`。

### 21. 更新：个人负责范围与 README 分离

- 需求：小组整体说明与个人负责内容分开维护，避免把共享原型中的全部能力算作个人交付。
- 处理：恢复根 `README.md` 和 `ad-ouc-prototype/README.md` 为整体项目 / 基础原型说明；新增 `README2.md` 专门记录个人模块。
- `README2.md` 明确负责患者基础信息采集、表格导入与缺失补填、SCD-Q9 患者自评、相关适老化 UI、草稿和接口契约。
- 明确排除医生专业量表、家属专业量表、正式诊断、后端实现、OCR、二进制 `.xlsx` 和生产级数据合规。
- 纠正文档口径：基础信息咨询当前为填表说明与校验；固定 FAQ 只有接口契约，尚未接入前端 / 后端；不声称已实现在线问诊。
- 补充文件边界、流程图、本人接口列表、共享代码调用限制、验收步骤和当前限制。
- 将与个人模块相关的技术框架、浏览器 API、未接入外部能力及核心按钮状态表集中迁入 `README2.md`，并标明用途和参考链接。

## 验证记录

- `node --check ad-ouc-prototype/app.js`：通过。
- `git diff --check`：通过。
- `Invoke-WebRequest http://127.0.0.1:8123/`：返回 `200`。
- 当前原型为纯前端本地演示，浏览器自动化环境无法稳定打开 localhost，截图级视觉回归需由本机浏览器完成。

## 当前运行方式

```powershell
cd C:\Users\novapss\Downloads\ad-ouc\ad-ouc-prototype
python -m http.server 8123
```

本机无 Python 时，可使用 Node.js 静态服务；然后打开：

```text
http://127.0.0.1:8123/
```
